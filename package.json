{
  "name": "mathquest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MathQuest — a friendly, offline, interactive math tutor for grades 3–6 with lessons, step-by-step explanations, badges and rewards.",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "serve": "npx --yes serve -l 8000 .",
    "test": "node docs/smoke.mjs && node docs/mastery.mjs",
    "pretest": "npm i --no-save jsdom@^24"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
